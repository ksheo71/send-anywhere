name: deploy
on:
  push: { branches: [main] }
  workflow_dispatch:
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - run: /opt/stack/services/public/myazit.kr/send-anywhere/repo/scripts/deploy.sh
